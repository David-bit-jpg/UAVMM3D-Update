# P1：只用重采的 PowerPlant 真机尺寸数据训练（ResNet8x，与 S1 同网络同损失）。
_BASE_CONFIG_: cfgs/models/uavdet_3d/camnorm/_model.yaml
DATA_CONFIG:
    _BASE_CONFIG_: cfgs/dataset_configs/uavdet_3d/camnorm_sim_pp_realsize.yaml
MODEL:
    POST_PROCESSING:
      MAX_OBJ: 5
